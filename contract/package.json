{
  "name": "w3s-contract",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "initCodeHash": "hardhat compile && hardhat run ./scripts/printInitCodeHash.ts",
    "clean": "hardhat clean",
    "build": "hardhat compile",
    "coverage": "hardhat coverage && open ../coverage/index.html",
    "test": "hardhat test",
    "deploy:dev": "hardhat deploy --tags full --network goerlyArbitrum",
    "deploy:arbitrum": "hardhat deploy --tags full --network arbitrumOne"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "@openzeppelin/contracts": "^4.8.0",
    "@openzeppelin/merkle-tree": "^1.0.1",
    "dotenv": "^16.0.3",
    "hardhat": "^2.12.2",
    "hardhat-deploy": "^0.11.20",
    "hardhat-erc1820": "^0.1.0",
    "hardhat-gas-reporter": "^1.0.9"
  }
}
